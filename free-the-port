#!/usr/bin/env bash
PORT=$1

if [ -z "$PORT" ]; then
  echo "Usage: $0 <port-number>"
  exit 1
fi

PID=$(lsof -ti :"$PORT")

if [ -z "$PID" ]; then
  echo "Port $PORT is already free"
else
  echo "Killing process $PID on port $PORT"
  kill -9 $PID && echo "Port $PORT is now free"
fi

